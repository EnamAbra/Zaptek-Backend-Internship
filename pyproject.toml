[project]
name = "zaptek"
version = "0.1.0"
description = ""Python projects and backend  assignments""
readme = "README.md"
authors = [
    { name = "EnamAbra", email = "ebene4321@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.141.1",
]

[project.scripts]
zaptek = "zaptek:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
